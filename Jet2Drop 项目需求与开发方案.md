# Jet2Drop 项目需求与开发方案

> 文档状态：当前真实实现基线
> 更新日期：2026-09-15
> 应用版本：`0.0.0+0`（正式发布前不变）

## 1. 项目范围

Jet2Drop 是 Windows、macOS、Android 共用的个人文件仓库客户端。Windows 保存实际仓库并运行 SFTPGo，三端通过同一 Tailscale Tailnet 访问。

当前交付范围包括：

- SFTPGo 仓库连接、目录浏览、刷新和排序；
- 新建与删除文件夹、上传、下载、删除；
- Finder/资源管理器拖入上传；
- 同名文件覆盖或保留两份；
- 常见图片和小型 UTF-8 文本预览；
- 传输任务暂停、继续、取消、重试和清理；
- `直连快传`与`可靠中转`两种普通文件传输路径；
- Windows、macOS 的默认快传保存目录；
- macOS 安全作用域书签；
- Tailscale 未连接时的识别、重试和唤起；
- Windows、macOS 关闭窗口后后台驻留，以及托盘/菜单栏退出。

当前明确不实现：

- Rclone 整仓双向自动同步；
- Mac 保存完整仓库本地副本；
- Windows 与 Mac 同时修改时自动生成冲突副本；
- 二维码配对；
- 面向其他用户分发的 Apple 签名和公证；
- 为直连增加分片、并行通道、断点续传或传输中途换路；
- Android 直连接收。

## 2. 设备角色与拓扑

```text
                     Tailscale Tailnet

 Android ─────── HTTP 直连发送 ──────> Windows
    │                                      ▲
    └──────── HTTP 直连发送 ──────> macOS │
                                      ▲   │
 Windows <──────── HTTP 直连 ─────────┘   │
    │                                     │
    └──────────── HTTP 直连 ──────────────┘

 Windows/SFTPGo 仓库
    ▲              ▲              ▲
    └── Windows ───┴── macOS ─────┴── Android
            SFTP/本地可靠中转与仓库访问
```

平台能力：

| 平台 | 直连发送 | 直连接收 | 可靠中转发送/领取 | 仓库访问 |
|---|---:|---:|---:|---:|
| Windows | 是 | 是 | 是 | 是（本地目录） |
| macOS | 是 | 是 | 是 | 是（SFTP） |
| Android | 是 | 暂不支持 | 是 | 是（SFTP） |

Windows 关机时，仓库和可靠中转不可用，但在线设备之间的直连仍可工作，例如 Android → macOS。目标桌面端 Jet2Drop 必须在线，直连才可使用。

Jet2Drop 不读取 Tailscale 的设备列表。应用通过可靠中转的设备登记记录发现 Jet2Drop 设备，将非敏感设备资料缓存在本机，并在发送前对目标的 HTTP 接收服务进行带令牌的健康探测。

## 3. 快传模式

快传页在同一行提供两个圆点选项，文字固定为：

- `直连快传`
- `可靠中转`

初始默认选择`直连快传`。当目标不支持某种路径时，不显示无法执行的选项；第一阶段向 Android 发送只显示`可靠中转`。

### 3.1 直连快传

直连用于在线设备之间即时发送任意单个文件：

- 通过 Tailscale 地址上的 HTTP 字节流传输；
- 不要求 Windows/SFTPGo 在线；
- 不写入仓库或中转收件箱；
- Windows、macOS 接收到默认快传保存目录；
- 每个文件是一项独立任务；多选文件由客户端创建多项任务；
- 支持取消；失败后只能从头重试；
- 不支持暂停、继续、断点续传、离线领取或应用重启恢复；
- 任务界面不显示上述不支持的操作。

发送前先读取文件大小并计算 SHA-256，再发起一次 HTTP 请求。请求携带文件名、大小、MIME 类型、SHA-256 和目标设备令牌。接收端：

1. 验证令牌、路径、方法、文件名、长度和 SHA-256 元数据；
2. 检查默认保存目录存在且可写；
3. 选择不冲突的目标名称；
4. 写入唯一 `.part` 临时文件并同时计算 SHA-256；
5. 校验实际大小和哈希；
6. 校验成功后原子改名；
7. 失败或取消时清理 `.part`。

通用接口为 `/v1/direct`，健康接口为 `/v1/direct/health`。桌面接收器继续兼容旧 `/v1/photo` 照片接口；旧接口仍进行图片扩展名和 MIME 校验。

接收服务使用固定端口 `37123`，只允许绑定 Tailscale IPv4 或测试使用的回环地址，不绑定公网或通配地址。本机直连令牌和已发现设备令牌的本地持久化仅使用系统安全存储；普通首选项只保存非敏感元数据。设备登记文件通过受 Tailscale 和 SFTP 鉴权保护的仓库交换接收端令牌。安全存储不可用时禁用直连接收并显示错误，不降级为本机明文存储。

### 3.2 可靠中转

可靠中转使用 Windows 仓库中的保留目录：

```text
__jet2drop_transfer/
├── devices/
└── messages/
    └── <transfer-id>/
        ├── payload.bin
        ├── manifest.json
        └── receipt.json
```

实际目录位于 SFTPGo 账号所暴露仓库根目录内。它不显示在普通仓库文件列表中，也不需要额外假设一个 `D:\Jet2DropTransfer` 虚拟目录。

每项中转任务包含一个文件：

- `payload.bin`：文件内容；上传期间使用稳定临时文件保护；
- `manifest.json`：发送完成后发布的任务描述；
- `receipt.json`：接收成功后的领取回执。

清单记录任务 ID、原文件名、大小、SHA-256、创建时间、过期时间、块大小、发送设备和目标设备。默认有效期是 24 小时。当前实现没有多文件 manifest、分块清单、`manifest.ready.json` 或 `acknowledgement.json`。

可靠中转支持：

- 目标设备稍后上线领取；
- 暂停、继续和取消；
- 失败重试；
- 应用重启后恢复未完成任务；
- 下载完成后的大小与 SHA-256 校验；
- `.part`、校验后发布和失败清理；
- 领取状态同步及删除记录后不反复出现。

SFTP 下载保持 64 KiB 读取块和最多 128 个待处理请求，并复用序列化仓库连接，以避免高延迟或 DERP 中转网络被大量往返限制速度。

### 3.3 中转降级规则

用户选择`可靠中转`后，在创建任务和上传之前检查仓库根目录以及可用空间（服务端支持 `statvfs` 时）。只有明确的网络不可达、连接超时、主机不可达或连接被拒绝，才探测目标直连接收服务并切换为直连。

以下情况不降级：

- 尚未配置 SFTPGo；
- 密码、身份验证或主机指纹错误；
- 文件或目录权限错误；
- 保存目录、磁盘或空间错误；
- 目标不支持直连；
- 传输已经开始。

若中转明确离线且目标直连探测也失败，则在创建任务前报告不可用，不保留一个实际无法执行的中转任务。

## 4. 仓库传输

Windows 使用本地仓库适配器；macOS 和 Android 使用同一套 `dartssh2` SFTP 适配器。仓库文件上传和下载均使用临时文件、完整性校验以及校验后的安全发布。

支持的用户流程：

- 进入文件夹、返回上级、刷新；
- 按名称、类型、大小、修改时间排序和切换升降序；
- 新建和删除文件夹；
- 文件选择上传与桌面拖入上传；
- 同名覆盖或保留两份；
- 下载并选择保存位置；
- 图片和小型文本预览；
- 删除文件；
- 多任务队列及暂停任务后让出执行机会。

普通仓库下载由用户选择保存位置。桌面快传领取和直连接收使用记忆的默认目录。macOS 通过安全作用域书签在重启后恢复该目录权限。

## 5. 设备登记与在线状态

可靠中转目录的 `devices` 子目录保存设备登记。应用连接仓库后周期刷新登记；近期登记用于发现收件人，过旧登记会清理。

本机缓存设备时：

- 设备 ID、名称、平台、端点和能力属于非敏感元数据；
- 直连令牌单独存入系统安全存储；
- 端点或能力变化时丢弃旧令牌，避免把旧凭据用于新服务；
- 仓库暂时离线时保留已知设备，但“可直连”由发送前健康探测决定；
- Android 永远不会因旧缓存被误判为可接收直连。

快传页面可以在仓库离线时打开。仓库离线只影响设备登记刷新、收件箱和可靠中转，不应阻塞已缓存桌面设备之间的直连。

## 6. 桌面生命周期

Windows 和 macOS 关闭主窗口时应用继续后台运行，以维持直连接收和传输任务：

- Windows 保留系统托盘图标；
- macOS 保留菜单栏或 Dock 可重新激活入口；
- 再次点击应用必须能恢复主窗口；
- 彻底退出只放在托盘/菜单栏图标的菜单中，不放在应用设置页面；
- 彻底退出时停止直连接收服务、网络连接和后台任务。

## 7. Tailscale 与连接恢复

仓库设置使用 Windows 的 Tailscale IP 或 MagicDNS 名称、SFTPGo 端口、账号、密码和真实主机指纹。密码及令牌不提交 Git。

连接失败只有在错误明确指向 Tailscale 连通性时才尝试打开 Tailscale。点击重试不会无条件唤起 Tailscale；鉴权、指纹、权限和配置错误应直接显示原始问题。Tailscale 恢复、Wi-Fi 切换、休眠唤醒或应用重新激活后，客户端重新建立仓库连接并维护直连接收服务。

## 8. 平台与构建基线

- Flutter `3.44.9`；
- Dart 使用 Flutter 内置版本；
- `dartssh2 2.22.5`；
- Android 最低 API 24；
- macOS 最低版本 12，目标为 Apple Silicon；
- Windows 10/11；
- 版本固定为 `0.0.0+0`。

构建命令：

```bash
flutter pub get
flutter analyze
flutter test
flutter build macos --release
flutter build apk --release
flutter build windows --release
```

各平台 Release 必须在对应平台构建；macOS 不能替代 Windows Release 构建验证。

## 9. 验证要求

自动化测试使用可明确清理的临时文件，覆盖：

- 路径清理、SHA-256、临时文件和原子发布；
- 直连任意文件、健康探测、鉴权、重名和失败清理；
- 旧照片接口兼容；
- 模式、路由与任务能力约束；
- 仅网络错误允许中转降级；
- 设备缓存与敏感令牌分离；
- 仓库与快传状态刷新、删除和多任务行为；
- 关键界面回归。

真机验收至少覆盖：

- 三端 SFTPGo 连接、重连和配置恢复；
- Windows ↔ macOS、Android → Windows、Android → macOS 直连；
- Windows 关机时 Android → macOS 直连；
- 发往 Android 只显示可靠中转；
- 中转离线领取、暂停继续、取消重试和重启恢复；
- 默认目录修改、重启权限、重名和 `.part` 清理；
- Windows 托盘与 macOS 后台驻留、窗口恢复和彻底退出；
- Tailscale 直连与 DERP、中断恢复、休眠唤醒和 Wi-Fi 切换；
- Release 产物独立启动。

涉及真实照片、视频或重要文件的步骤由用户亲自执行。代码、自动化测试或已有构建产物不能替代真机验收结论。

## 10. 发布约束

- 当前仅供自用，不做公开分发签名与公证；
- 未经用户确认不提交或推送代码；
- 不改变现有仓库和可靠中转语义；
- 不降低完整性校验和失败保护；
- 不把密码、令牌、主机密钥或私密连接记录提交到 Git；
- 每次修复后运行相关测试，最终运行全量 analyze、test 和可用平台 Release 构建。
