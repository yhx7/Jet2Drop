# Jet2Drop 协作约定

- 凡是工作量大、适合独立执行且要求尽量不出错的实现、重构或全量测试，必须优先交给 `gpt-5.6-luna`、`max` 推理强度的子代理执行。
- 主代理负责理解需求、拆分边界、审查改动、补充真实设备验收、定位遗漏与最终调试，不直接包办上述大任务。
- 小范围排查、验收、收尾和紧急修复可由主代理直接完成，避免为了形式增加不必要的协调成本。
- 未真实发布前，应用版本始终保持 `0.0.0+0`。
- Android 文件删除入口保持用户设计的滑动删除，不擅自改成交互按钮。
- 涉及用户真实文件内容或主观界面观感的检查交给用户；自动化测试只使用可明确清理的临时测试文件。
